# LocalChat 安全说明

本文描述 LocalP2PChat 的安全模型、每条攻击路径上的防护，以及部署信令服务器时的注意事项。

## 威胁模型与防护

### 传输层（群聊 / 直聊 / 通话 / 文件）

| 攻击 | 防护 |
| --- | --- |
| 被动嗅探 | 所有 TCP 连接先经加密握手，之后每行均为 `nonce ‖ AES-256-GCM(JSON)`；明文包一律拒绝，无降级路径。握手本身（临时 ECDH P-256 + PBKDF2-SHA1 210k 绑定群密码 + 双向 HMAC）不泄露密码。 |
| 主动中间人（群） | 会话密钥 = HKDF(ECDH ‖ pwKey)：不知道群密码就无法代入握手或解密；HMAC 绑定双方临时公钥。群密码为加密随机 8 位（≈47.6 bit），离线爆破不可行。 |
| 主动中间人（直聊/通话） | 双方长期身份密钥（ECDSA P-256）签名握手 transcript；TOFU 记录对端密钥，变更即拒绝并提示。**首次接触时的中间人无法被自动检测**——接受请求前请核对请求卡片上显示的对方安全码（设置页可查看本机安全码）。 |
| 重放 / 注入 | 每方向数据包携带严格递增的会话序号（GCM 保护内），接收端只接受 `last+1`；重放、乱序、注入一律断连。另有每连接 nonce 缓存作纵深防御。兼容旧版本（不携带序号）对端：其报文持续放行，直到对端首次携带序号后转为强制校验；旧对端仍受每连接 nonce 缓存保护。 |
| 文件窃取 | 文件字节走独立短命下载端口，meta 与每个分块均以 per-file 随机密钥加密；下载请求必须携带 `HMAC(fileKey, fileId)` 令牌（证明收到过加密的文件消息），缺失或不符一律不回任何字节。下载 512MB 封顶，文件名清洗防路径穿越。 |
| 伪造消息 / 越权删除 | 群消息校验 senderId 与连接身份一致；删除仅限原作者（主机与 mesh 双侧校验）；呼叫信令点对点路由并校验参与者。 |
| CPU 耗尽 / 握手风暴 | 所有监听端口：并发连接上限（Windows 256 / Android 64）+ 每 IP 握手限流（60 次/分钟）；每连接握手一次 PBKDF2，有界。 |

### 信令/中继服务器（可选部署）

- 服务器只见密文：中继模式是原始字节管道，配对后双方执行端到端加密握手，服务器无法解密任何业务流量。
- **访问认证（协议 v2）**：每条控制连接必须先通过质询-应答认证（`HMAC-SHA256(访问密钥, 服务器新鲜 nonce)`），否则不能注册、配对或占用中继。证明一次性，密钥永不上线。
- 服务器资源有界：总连接 512、每 IP 32、每连接群注册 ≤32、全部状态有超时回收。

**部署要求**：

```bash
# 推荐：显式指定访问密钥（客户端在“设置 → 中继/打洞服务器”填同一密钥）
python3 signaling_server.py 25000 --secret '<长随机串>'
# 或从环境变量读取
LOCALCHAT_SIGNALING_SECRET='<长随机串>' python3 signaling_server.py
# 未提供时启动会自动生成一个并打印一次
```

建议再套 TLS（如 stunnel/nginx stream）或 SSH 隧道，防止连接元数据（群号、通信时间、对端地址）被链路观察者收集。服务器与客户端之间的地址交换本身按设计是明文 JSON（打洞需要），业务内容不受影响。

### 落盘（设备失窃/本机文件被拷走）

- 设备身份私钥：Windows DPAPI 加密（`identity.json`）；Android 存于 AndroidKeyStore 包裹的 SharedPreferences。
- 群密码、聊天内容、最后消息预览：Windows 以安装级内容密钥（DPAPI 包裹）AES-GCM 加密入库；Android 以 AndroidKeyStore 密钥包裹（`enc1:` 前缀，明文值可透读以兼容旧数据）。
- Android `allowBackup=false`；导出组件最小化。

已知剩余风险（有意接受的权衡）：

1. **直聊首次接触的 TOFU**：攻击者在双方第一次互加时介入无法被自动发现。缓解：请求卡片显示对方安全码，接受前人工比对。
2. **握手元数据**：`hs_start` 中的群号/模式为明文（避免对每个候选群做 PBKDF2 的开销），链路观察者可知"有此类连接"。业务内容不受影响。
3. KeyStore/DPAPI 不可用时落盘加密自动降级为明文（日志有告警）——优先保证身份与历史可用。

## 密码学原语

- AEAD：AES-256-GCM（随机 12B nonce，128bit tag），布局 `nonce ‖ ct ‖ tag`
- 密钥协商：临时 ECDH P-256（共享秘密最小编码规范化，跨平台一致）
- 密码绑定：PBKDF2-HMAC-SHA1，210k 迭代（Android API 24 全兼容），HKDF-SHA256 派生会话密钥
- 身份认证：ECDSA P-256（SHA256withECDSA）签名握手 transcript；指纹 = SHA256(SPKI DER) 前 16 hex（"安全码"）
- 常数时间比较：MAC/令牌校验全部使用非常数时间安全实现
